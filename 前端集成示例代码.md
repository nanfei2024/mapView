# 🔧 前端集成示例代码

## 📝 说明

本文档提供**完整的前端代码示例**，展示如何将文档数字化与文件管理系统集成。

---

## 📁 API服务封装

### 1. 创建 `src/api/integration.ts`

```typescript
import axios from 'axios';
import { ElMessage } from 'element-plus';

// API基础配置
const API_BASE = {
  document: import.meta.env.VITE_DOCUMENT_API || 'http://localhost:8080/api/documents',
  image: import.meta.env.VITE_IMAGE_API || 'http://localhost:8081/api/images',
  table: import.meta.env.VITE_TABLE_API || 'http://localhost:8082/api/tables'
};

// 响应数据类型
interface ApiResponse<T = any> {
  code: number;
  msg: string;
  data?: T;
  error?: string;
}

// ===== 文档管理 =====

export interface SaveDigitalizationRequest {
  fileName: string;
  fileType: string;
  fileSize: number;
  originalUrl: string;
  resultUrl: string;
  mineruTaskId: string;
  status: string;
}

export interface SaveDigitalizationResponse {
  documentId: string;
  extractedImages: number;
  extractedTables: number;
}

/**
 * 保存数字化处理结果
 */
export const saveDigitalizationResult = async (
  data: SaveDigitalizationRequest
): Promise<SaveDigitalizationResponse> => {
  try {
    const response = await axios.post<ApiResponse<SaveDigitalizationResponse>>(
      `${API_BASE.document}/from-digitalization`,
      data
    );

    if (response.data.code === 0 && response.data.data) {
      return response.data.data;
    } else {
      throw new Error(response.data.msg || '保存失败');
    }
  } catch (error: any) {
    console.error('保存数字化结果失败:', error);
    throw new Error(error.response?.data?.msg || error.message || '保存失败');
  }
};

/**
 * 获取文档列表
 */
export const getDocumentList = async (params: {
  page: number;
  pageSize: number;
  fileType?: string;
  source?: string;
}) => {
  try {
    const response = await axios.get<ApiResponse>(`${API_BASE.document}`, { params });
    if (response.data.code === 0) {
      return response.data.data;
    } else {
      throw new Error(response.data.msg);
    }
  } catch (error: any) {
    console.error('获取文档列表失败:', error);
    throw error;
  }
};

// ===== 图库管理 =====

/**
 * 获取图片列表
 */
export const getImageList = async (params: {
  page: number;
  pageSize: number;
  category?: string;
  source?: string;
}) => {
  try {
    const response = await axios.get<ApiResponse>(`${API_BASE.image}`, { params });
    if (response.data.code === 0) {
      return response.data.data;
    } else {
      throw new Error(response.data.msg);
    }
  } catch (error: any) {
    console.error('获取图片列表失败:', error);
    throw error;
  }
};

/**
 * 上传图片
 */
export const uploadImage = async (file: File, category: string) => {
  try {
    const formData = new FormData();
    formData.append('file', file);
    formData.append('category', category);

    const response = await axios.post<ApiResponse>(
      `${API_BASE.image}/upload`,
      formData,
      {
        headers: {
          'Content-Type': 'multipart/form-data'
        }
      }
    );

    if (response.data.code === 0) {
      return response.data.data;
    } else {
      throw new Error(response.data.msg);
    }
  } catch (error: any) {
    console.error('上传图片失败:', error);
    throw error;
  }
};

// ===== 表库管理 =====

/**
 * 获取表格列表
 */
export const getTableList = async (params: {
  page: number;
  pageSize: number;
  category?: string;
  tableType?: string;
}) => {
  try {
    const response = await axios.get<ApiResponse>(`${API_BASE.table}`, { params });
    if (response.data.code === 0) {
      return response.data.data;
    } else {
      throw new Error(response.data.msg);
    }
  } catch (error: any) {
    console.error('获取表格列表失败:', error);
    throw error;
  }
};

/**
 * 导入表格
 */
export const importTable = async (
  file: File,
  data: {
    tableName: string;
    category: string;
    description?: string;
  }
) => {
  try {
    const formData = new FormData();
    formData.append('file', file);
    formData.append('tableName', data.tableName);
    formData.append('category', data.category);
    if (data.description) {
      formData.append('description', data.description);
    }

    const response = await axios.post<ApiResponse>(
      `${API_BASE.table}/import`,
      formData,
      {
        headers: {
          'Content-Type': 'multipart/form-data'
        }
      }
    );

    if (response.data.code === 0) {
      return response.data.data;
    } else {
      throw new Error(response.data.msg);
    }
  } catch (error: any) {
    console.error('导入表格失败:', error);
    throw error;
  }
};
```

---

## 🎯 文档数字化页面集成

### 2. 修改 `src/views/DocumentDigitalizationPage.vue`

在现有代码中添加以下部分：

```typescript
// 导入集成API
import {
  saveDigitalizationResult,
  type SaveDigitalizationRequest
} from '../api/integration';

// 在任务完成后调用保存
const handleTaskComplete = async (file: UploadedFile) => {
  console.log('任务完成，开始保存到文件管理...', file);

  try {
    // 准备保存数据
    const saveData: SaveDigitalizationRequest = {
      fileName: file.name,
      fileType: file.type || 'pdf',
      fileSize: file.size || 0,
      originalUrl: file.sourceUrl || '',
      resultUrl: file.resultUrl || '',
      mineruTaskId: file.taskId || '',
      status: 'completed'
    };

    // 调用保存API
    const result = await saveDigitalizationResult(saveData);

    // 显示成功消息
    const message = `
      文档处理完成！
      ✓ 文档已保存到文件列表
      ✓ 提取了 ${result.extractedImages} 张图片到图库
      ✓ 提取了 ${result.extractedTables} 个表格到表库
    `;

    ElMessageBox.confirm(
      message,
      '数字化成功',
      {
        confirmButtonText: '查看文件管理',
        cancelButtonText: '继续处理',
        type: 'success',
        dangerouslyUseHTMLString: false
      }
    ).then(() => {
      // 跳转到文件管理中心，并高亮显示新文档
      router.push({
        path: '/file-list',
        query: { 
          highlight: result.documentId,
          tab: 'files'  // 默认显示文件列表标签
        }
      });
    }).catch(() => {
      // 用户选择继续处理，不做任何操作
      console.log('用户选择继续处理其他文件');
    });

  } catch (error: any) {
    console.error('保存到文件管理失败:', error);
    ElMessage.error({
      message: `保存失败: ${error.message}`,
      duration: 5000
    });
  }
};

// 修改 pollTaskUntilComplete 函数，在完成时调用 handleTaskComplete
const pollTaskUntilComplete = async (
  taskId: string,
  file: UploadedFile,
  maxAttempts = 60
): Promise<void> => {
  // ... 现有的轮询逻辑 ...

  if (result.data.state === 'done') {
    file.status = 'completed';
    file.resultUrl = result.data.full_zip_url;
    ElMessage.success(`✅ ${file.name} 处理完成！`);
    
    // 🎯 关键：调用保存函数
    await handleTaskComplete(file);
    
    return;
  }

  // ... 其余代码保持不变 ...
};
```

---

## 📋 文件管理页面集成

### 3. 修改 `src/views/FileListPage.vue`

添加高亮新文档的功能：

```typescript
<script setup lang="ts">
import { ref, onMounted, nextTick } from 'vue';
import { useRouter, useRoute } from 'vue-router';
import FilePagination from '../components/FilePagination.vue';
import ImageGallery from '../components/ImageGallery.vue';
import TableLibrary from '../components/TableLibrary.vue';

const router = useRouter();
const route = useRoute();

// 当前激活的标签页
const activeTab = ref<'files' | 'images' | 'tables'>('files');

// 返回上一页
const goBack = () => {
  router.back();
};

// 处理高亮显示
onMounted(async () => {
  // 从URL获取参数
  const highlightId = route.query.highlight as string;
  const tabParam = route.query.tab as string;

  // 如果指定了标签，切换到该标签
  if (tabParam && ['files', 'images', 'tables'].includes(tabParam)) {
    activeTab.value = tabParam as 'files' | 'images' | 'tables';
  }

  // 如果有要高亮的文档
  if (highlightId) {
    // 等待DOM更新
    await nextTick();
    
    // 延迟一下确保组件完全加载
    setTimeout(() => {
      // 查找并高亮显示元素
      const element = document.querySelector(`[data-document-id="${highlightId}"]`);
      
      if (element) {
        // 滚动到视图中
        element.scrollIntoView({ 
          behavior: 'smooth', 
          block: 'center' 
        });
        
        // 添加高亮类
        element.classList.add('highlight-new');
        
        // 3秒后移除高亮
        setTimeout(() => {
          element.classList.remove('highlight-new');
        }, 3000);
      }
    }, 500);
  }
});
</script>

<style scoped>
/* ... 现有样式 ... */

/* 新增：高亮动画 */
@keyframes highlight {
  0%, 100% {
    background-color: transparent;
  }
  50% {
    background-color: rgba(102, 126, 234, 0.2);
  }
}

:deep(.highlight-new) {
  animation: highlight 1.5s ease-in-out 2;
  border: 2px solid #667eea !important;
  box-shadow: 0 0 20px rgba(102, 126, 234, 0.4) !important;
}
</style>
```

---

## 🖼️ 图库组件集成

### 4. 修改 `src/components/ImageGallery.vue`

将模拟数据替换为真实API调用：

```typescript
import { ref, computed, onMounted } from 'vue';
import { ElMessage, ElMessageBox } from 'element-plus';
import { getImageList, uploadImage } from '../api/integration';

// 状态管理
const images = ref<ImageData[]>([]);
const loading = ref(false);
const currentPage = ref(1);
const pageSize = ref(12);
const totalImages = ref(0);

// 加载图片列表
const loadImages = async () => {
  loading.value = true;
  try {
    const response = await getImageList({
      page: currentPage.value,
      pageSize: pageSize.value,
      category: categoryFilter.value || undefined,
      source: undefined
    });

    images.value = response.records;
    totalImages.value = response.total;
  } catch (error: any) {
    ElMessage.error('加载图片列表失败: ' + error.message);
  } finally {
    loading.value = false;
  }
};

// 上传图片
const handleUpload = async () => {
  // 创建文件选择器
  const input = document.createElement('input');
  input.type = 'file';
  input.accept = 'image/*';
  input.multiple = false;

  input.onchange = async (e: Event) => {
    const target = e.target as HTMLInputElement;
    const file = target.files?.[0];
    
    if (!file) return;

    try {
      loading.value = true;
      
      // 默认分类为"其他"
      const result = await uploadImage(file, '其他');
      
      ElMessage.success('上传成功！');
      
      // 重新加载列表
      await loadImages();
    } catch (error: any) {
      ElMessage.error('上传失败: ' + error.message);
    } finally {
      loading.value = false;
    }
  };

  input.click();
};

// 组件挂载时加载数据
onMounted(() => {
  loadImages();
});

// 监听分页变化
const handlePageSizeChange = () => {
  currentPage.value = 1;
  loadImages();
};

const goToPage = (page: number) => {
  currentPage.value = page;
  loadImages();
};

// 监听筛选变化
const handleFilter = () => {
  currentPage.value = 1;
  loadImages();
};
```

---

## 📊 表库组件集成

### 5. 修改 `src/components/TableLibrary.vue`

类似地，将模拟数据替换为真实API：

```typescript
import { ref, computed, onMounted } from 'vue';
import { ElMessage, ElMessageBox } from 'element-plus';
import { getTableList, importTable } from '../api/integration';

// 状态管理
const tables = ref<TableData[]>([]);
const loading = ref(false);
const currentPage = ref(1);
const pageSize = ref(9);
const totalTables = ref(0);

// 加载表格列表
const loadTables = async () => {
  loading.value = true;
  try {
    const response = await getTableList({
      page: currentPage.value,
      pageSize: pageSize.value,
      category: categoryFilter.value || undefined,
      tableType: typeFilter.value || undefined
    });

    tables.value = response.records;
    totalTables.value = response.total;
  } catch (error: any) {
    ElMessage.error('加载表格列表失败: ' + error.message);
  } finally {
    loading.value = false;
  }
};

// 导入表格
const handleImport = async () => {
  // 创建文件选择器
  const input = document.createElement('input');
  input.type = 'file';
  input.accept = '.xlsx,.xls,.csv,.json';
  input.multiple = false;

  input.onchange = async (e: Event) => {
    const target = e.target as HTMLInputElement;
    const file = target.files?.[0];
    
    if (!file) return;

    try {
      loading.value = true;
      
      // 提取文件名作为表格名称
      const tableName = file.name.replace(/\.[^/.]+$/, '');
      
      const result = await importTable(file, {
        tableName: tableName,
        category: '其他',
        description: '手动导入的表格'
      });
      
      ElMessage.success('导入成功！');
      
      // 重新加载列表
      await loadTables();
    } catch (error: any) {
      ElMessage.error('导入失败: ' + error.message);
    } finally {
      loading.value = false;
    }
  };

  input.click();
};

// 组件挂载时加载数据
onMounted(() => {
  loadTables();
});

// 监听分页和筛选变化
const handlePageSizeChange = () => {
  currentPage.value = 1;
  loadTables();
};

const goToPage = (page: number) => {
  currentPage.value = page;
  loadTables();
};

const handleFilter = () => {
  currentPage.value = 1;
  loadTables();
};
```

---

## 📄 FilePagination组件集成

### 6. 修改 `src/components/FilePagination.vue`

```typescript
import { ref, onMounted } from 'vue';
import { useRoute } from 'vue-router';
import { ElMessage } from 'element-plus';
import { getDocumentList } from '../api/integration';

const route = useRoute();

// 状态管理
const files = ref<any[]>([]);
const loading = ref(false);
const currentPage = ref(1);
const pageSize = ref(10);
const total = ref(0);

// 加载文件列表
const fetchFiles = async () => {
  loading.value = true;
  try {
    const response = await getDocumentList({
      page: currentPage.value,
      pageSize: pageSize.value,
      fileType: fileTypeFilter.value || undefined,
      source: undefined
    });

    files.value = response.records;
    total.value = response.total;
  } catch (error: any) {
    ElMessage.error('加载文件列表失败: ' + error.message);
  } finally {
    loading.value = false;
  }
};

// 组件挂载时加载
onMounted(() => {
  fetchFiles();
});
```

---

## 🎯 完整调用流程

### 用户操作流程

```
1. 用户在"地质文档数字化"页面上传PDF
   ↓
2. MinerU API 处理文档
   ↓
3. 处理完成后调用 saveDigitalizationResult()
   ↓
4. 后端自动解析ZIP，提取图片和表格
   ↓
5. 前端显示成功提示，询问是否查看
   ↓
6. 用户点击"查看文件管理"
   ↓
7. 跳转到文件管理中心，高亮显示新文档
   ↓
8. 用户可以在三个标签中查看：
   - 📋 文件列表：看到新文档
   - 🖼️ 我的图库：看到提取的图片
   - 📊 我的表库：看到提取的表格
```

---

## 🔧 环境变量配置

### `.env.development`
```env
# 文档服务
VITE_DOCUMENT_API=http://localhost:8080/api/documents

# 图库服务
VITE_IMAGE_API=http://localhost:8081/api/images

# 表库服务
VITE_TABLE_API=http://localhost:8082/api/tables

# MinerU API
VITE_MINERU_API_TOKEN=your_token_here
```

---

## 🧪 测试步骤

### 1. 测试文档数字化

```typescript
// 在浏览器控制台测试
import { saveDigitalizationResult } from './api/integration';

const testData = {
  fileName: "测试文档.pdf",
  fileType: "pdf",
  fileSize: 1000000,
  originalUrl: "https://example.com/test.pdf",
  resultUrl: "https://example.com/result.zip",
  mineruTaskId: "test_task_001",
  status: "completed"
};

const result = await saveDigitalizationResult(testData);
console.log('保存结果:', result);
```

### 2. 测试列表加载

```typescript
// 测试获取文档列表
import { getDocumentList } from './api/integration';

const docs = await getDocumentList({ page: 1, pageSize: 10 });
console.log('文档列表:', docs);

// 测试获取图片列表
import { getImageList } from './api/integration';

const images = await getImageList({ page: 1, pageSize: 12 });
console.log('图片列表:', images);

// 测试获取表格列表
import { getTableList } from './api/integration';

const tables = await getTableList({ page: 1, pageSize: 9 });
console.log('表格列表:', tables);
```

---

## 🐛 调试技巧

### 1. 查看网络请求

打开浏览器开发者工具 → Network 标签，筛选 XHR 请求，查看：
- 请求URL是否正确
- 请求参数是否正确
- 响应状态码
- 响应数据

### 2. 添加日志

在关键位置添加 console.log：

```typescript
console.log('开始保存文档...', saveData);
const result = await saveDigitalizationResult(saveData);
console.log('保存成功，结果:', result);
```

### 3. 错误捕获

```typescript
try {
  await saveDigitalizationResult(data);
} catch (error) {
  console.error('详细错误信息:', {
    message: error.message,
    response: error.response,
    stack: error.stack
  });
}
```

---

## ✅ 集成检查清单

- [ ] 创建 `src/api/integration.ts` 文件
- [ ] 配置环境变量 `.env.development`
- [ ] 修改文档数字化页面，添加保存逻辑
- [ ] 修改文件管理页面，添加高亮逻辑
- [ ] 修改图库组件，连接真实API
- [ ] 修改表库组件，连接真实API
- [ ] 修改文件列表组件，连接真实API
- [ ] 测试完整流程
- [ ] 处理错误情况
- [ ] 添加加载状态

---

## 📞 需要帮助？

如果遇到集成问题，请检查：
1. ✅ 后端API是否正常运行
2. ✅ 环境变量是否配置正确
3. ✅ 网络请求是否成功
4. ✅ 数据格式是否匹配

**集成完成后，整个系统就可以正常工作了！** 🎉

