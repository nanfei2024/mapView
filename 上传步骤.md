# 📤 上传到 GitHub 的步骤

## 🚀 快速开始（推荐）

### 方法一：使用批处理脚本

1. **双击运行** `快速上传.bat` 文件
2. 按照提示输入提交信息，例如：
   ```
   feat: 添加智能问答和封面显示功能
   ```
3. 等待上传完成

---

## 📝 手动操作步骤

### 首次上传

#### 1. 检查是否已初始化 Git

打开命令行（在项目文件夹右键 → Git Bash Here 或 PowerShell）：

```bash
git status
```

如果提示 "not a git repository"，需要先初始化：

```bash
git init
```

#### 2. 在 GitHub 创建仓库

1. 登录 https://github.com
2. 点击右上角 `+` → `New repository`
3. 填写：
   - Repository name: `knowledge-graph-system`
   - Description: `书籍知识图谱可视化系统`
   - 选择 Public 或 Private
4. **不要勾选** "Initialize this repository with a README"
5. 点击 `Create repository`

#### 3. 关联远程仓库

```bash
git remote add origin https://github.com/你的用户名/knowledge-graph-system.git
```

#### 4. 添加文件

```bash
git add .
```

#### 5. 提交

```bash
git commit -m "feat: 初始提交 - 书籍知识图谱系统

- 添加知识图谱可视化功能
- 添加智能问答系统
- 添加节点编辑功能
- 书籍节点显示封面
- 添加完整的技术文档"
```

#### 6. 推送到 GitHub

```bash
git branch -M main
git push -u origin main
```

---

### 更新已有仓库

如果之前已经上传过，现在要更新：

```bash
# 1. 添加修改的文件
git add .

# 2. 提交
git commit -m "feat: 添加智能问答和封面显示功能

- 添加 KnowledgeQA 智能问答组件
- 书籍节点显示封面图片
- 添加节点跳转和高亮功能
- 完善技术文档（API、数据库、系统概述）
- 修复图谱初始化问题"

# 3. 推送
git push
```

---

## 🎯 本次更新内容建议的提交信息

```
feat: 添加智能问答系统和完善知识图谱功能

主要更新：
- ✨ 新增智能问答组件 (KnowledgeQA.vue)
  - 支持自然语言问答
  - 节点智能搜索
  - 一键跳转定位
  - 预留 AI 接口

- 🎨 优化知识图谱显示
  - 书籍节点显示封面图片
  - 节点跳转和高亮效果
  - 修复图表初始化问题

- ✏️ 完善节点编辑功能
  - 右键菜单操作
  - 节点增删改
  - 编辑模式切换

- 📚 添加完整技术文档
  - API 接口文档
  - 数据库设计文档
  - 系统概述文档
  - Git 上传指南

- 🐛 Bug 修复
  - 修复 getOption 返回 undefined 问题
  - 修复返回按钮无效问题
  - 优化节点位置保存逻辑
```

---

## ⚠️ 常见问题

### 问题 1：需要输入用户名密码

GitHub 已不支持密码登录，需要使用 Personal Access Token。

**解决方案**：

1. 访问 GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. 点击 `Generate new token (classic)`
3. 勾选 `repo` 权限
4. 生成并复制 token
5. 推送时：
   - Username: 你的 GitHub 用户名
   - Password: 粘贴刚才复制的 token

### 问题 2：推送被拒绝

```
! [rejected] main -> main (fetch first)
```

**解决方案**：

```bash
git pull origin main --rebase
git push origin main
```

### 问题 3：文件太大

如果有大文件（如图片、视频），确保它们在 `public/images/` 目录下，并且大小合理。

如果确实需要上传大文件：

```bash
git lfs install
git lfs track "*.jpg"
git lfs track "*.png"
git add .gitattributes
git commit -m "Add Git LFS"
git push
```

---

## ✅ 验证上传成功

1. 访问你的 GitHub 仓库页面
2. 检查文件是否都已上传
3. 查看提交历史
4. 确认文档显示正常

---

## 📋 需要上传的主要文件

### ✅ 源代码
- `src/views/HierarchicalGraphPage.vue` - 知识图谱主页面
- `src/components/KnowledgeQA.vue` - 智能问答组件
- `src/components/HierarchicalKnowledgeGraph.vue` - 嵌入式图谱
- `src/api/` - API 接口文件
- `src/router/index.ts` - 路由配置

### ✅ 文档
- `docs/API接口文档.md`
- `docs/数据库设计文档.md`
- `docs/系统概述文档.md`
- `智能问答功能说明.md`
- `编辑功能使用说明.md`
- `封面显示说明.md`
- `Git上传指南.md`

### ✅ 配置文件
- `package.json`
- `vite.config.ts`
- `tsconfig.json`
- `.gitignore`

### ❌ 不要上传
- `node_modules/` - 依赖包（太大）
- `dist/` - 构建产物
- `.env` - 环境变量（包含敏感信息）

---

## 💡 提示

1. **首次上传**：使用 `快速上传.bat` 前，需要先关联远程仓库
2. **后续更新**：直接双击 `快速上传.bat` 即可
3. **提交信息**：尽量写清楚，方便以后查看历史
4. **经常提交**：建议每完成一个功能就提交一次

---

## 🎉 完成后

上传成功后，您可以：

1. 在 GitHub 上查看代码
2. 分享仓库链接给团队成员
3. 设置 GitHub Pages 部署静态页面
4. 配置 CI/CD 自动部署

---

需要帮助？查看 `Git上传指南.md` 获取更详细的说明。
