# 📊 知识图谱集成方案

## 🎯 概述

基于现有的图书解析和目录细化功能，构建地质科学领域的知识图谱系统，实现从文档到结构化知识的自动提取和可视化。

---

## 📋 目录

1. [核心概念](#核心概念)
2. [数据模型设计](#数据模型设计)
3. [实体提取策略](#实体提取策略)
4. [关系识别方法](#关系识别方法)
5. [技术架构](#技术架构)
6. [存储方案](#存储方案)
7. [前端可视化](#前端可视化)
8. [API设计](#api设计)
9. [实施路线图](#实施路线图)
10. [示例场景](#示例场景)

---

## 🧠 核心概念

### 知识图谱的三要素

1. **实体（Entity）**：地质学中的核心概念
   - 地质构造（如：劳亚区系、冈瓦纳区系）
   - 地质现象（如：板块运动、造山运动）
   - 地理位置（如：特提斯陆间带、太平洋板块）
   - 地质年代（如：古生代、中生代）
   - 研究方法（如：构造地质学、地貌测量）

2. **关系（Relation）**：实体之间的连接
   - 层级关系：`属于`、`包含`、`位于`
   - 因果关系：`导致`、`影响`、`形成`
   - 时间关系：`发生于`、`早于`、`晚于`
   - 空间关系：`相邻`、`连接`、`分离`
   - 属性关系：`具有特征`、`属于类型`

3. **属性（Attribute）**：实体的特征信息
   - 名称、描述、时间、位置、类型等

---

## 🗄️ 数据模型设计

### 1. 实体类型（Entity Types）

```typescript
// 实体类型枚举
enum EntityType {
  // 地质构造
  GEOLOGICAL_STRUCTURE = 'geological_structure',  // 地质构造
  PLATE = 'plate',                               // 板块
  FAULT = 'fault',                               // 断层
  FOLD = 'fold',                                 // 褶皱
  
  // 地质现象
  GEOLOGICAL_PHENOMENON = 'geological_phenomenon', // 地质现象
  OROGENY = 'orogeny',                            // 造山运动
  MAGMATISM = 'magmatism',                        // 岩浆活动
  
  // 地理位置
  LOCATION = 'location',                          // 地理位置
  REGION = 'region',                              // 区域
  OCEAN = 'ocean',                                // 海洋
  
  // 地质年代
  GEOLOGICAL_AGE = 'geological_age',              // 地质年代
  ERA = 'era',                                    // 代
  PERIOD = 'period',                              // 纪
  
  // 研究方法
  RESEARCH_METHOD = 'research_method',            // 研究方法
  THEORY = 'theory',                              // 理论
  TECHNIQUE = 'technique',                        // 技术
  
  // 文档结构
  CHAPTER = 'chapter',                            // 章节
  SECTION = 'section',                            // 小节
  CONCEPT = 'concept'                             // 概念
}
```

### 2. 关系类型（Relation Types）

```typescript
// 关系类型枚举
enum RelationType {
  // 层级关系
  BELONGS_TO = 'belongs_to',           // 属于
  CONTAINS = 'contains',               // 包含
  PART_OF = 'part_of',                // 是...的一部分
  
  // 因果关系
  CAUSES = 'causes',                   // 导致
  INFLUENCES = 'influences',           // 影响
  FORMS = 'forms',                     // 形成
  RESULTS_IN = 'results_in',           // 产生
  
  // 时间关系
  OCCURS_IN = 'occurs_in',             // 发生于
  EARLIER_THAN = 'earlier_than',       // 早于
  LATER_THAN = 'later_than',           // 晚于
  CONTEMPORANEOUS = 'contemporaneous', // 同时期
  
  // 空间关系
  ADJACENT_TO = 'adjacent_to',         // 相邻
  CONNECTS = 'connects',               // 连接
  SEPARATES = 'separates',             // 分离
  LOCATED_IN = 'located_in',           // 位于
  
  // 属性关系
  HAS_FEATURE = 'has_feature',         // 具有特征
  IS_TYPE_OF = 'is_type_of',           // 是...的类型
  RELATED_TO = 'related_to',           // 与...相关
  
  // 文档关系
  MENTIONS = 'mentions',               // 提及
  DESCRIBES = 'describes',             // 描述
  REFERENCES = 'references'            // 引用
}
```

### 3. 数据库设计

#### 方案A：图数据库（推荐）

**使用 Neo4j 或 ArangoDB**

```cypher
// Neo4j 节点创建示例
CREATE (laurasia:GeologicalStructure {
  id: 'geo_001',
  name: '劳亚区系',
  name_en: 'Laurasia',
  description: '古生代晚期形成的超级大陆',
  type: 'geological_structure',
  created_at: datetime(),
  source_document: 'doc_001',
  source_chapter: '第一章'
})

CREATE (pangaea:GeologicalStructure {
  id: 'geo_002',
  name: '泛大陆',
  name_en: 'Pangaea',
  type: 'geological_structure'
})

// 创建关系
CREATE (laurasia)-[:PART_OF]->(pangaea)
CREATE (laurasia)-[:OCCURS_IN {era: '古生代'}]->(paleozoic:GeologicalAge {name: '古生代'})
```

#### 方案B：关系数据库（兼容现有系统）

```sql
-- 实体表
CREATE TABLE knowledge_entities (
    id VARCHAR(50) PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    name_en VARCHAR(255),
    type VARCHAR(50) NOT NULL,
    description TEXT,
    properties JSON,  -- 扩展属性
    source_document_id VARCHAR(50),
    source_chapter VARCHAR(100),
    source_section VARCHAR(100),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_type (type),
    INDEX idx_name (name),
    INDEX idx_source_document (source_document_id)
);

-- 关系表
CREATE TABLE knowledge_relations (
    id VARCHAR(50) PRIMARY KEY,
    source_entity_id VARCHAR(50) NOT NULL,
    target_entity_id VARCHAR(50) NOT NULL,
    relation_type VARCHAR(50) NOT NULL,
    properties JSON,  -- 关系属性（如时间、强度等）
    confidence FLOAT DEFAULT 1.0,  -- 置信度
    source_document_id VARCHAR(50),
    source_text TEXT,  -- 来源文本片段
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (source_entity_id) REFERENCES knowledge_entities(id),
    FOREIGN KEY (target_entity_id) REFERENCES knowledge_entities(id),
    INDEX idx_source (source_entity_id),
    INDEX idx_target (target_entity_id),
    INDEX idx_relation_type (relation_type)
);

-- 实体别名表（处理同义词）
CREATE TABLE entity_aliases (
    id VARCHAR(50) PRIMARY KEY,
    entity_id VARCHAR(50) NOT NULL,
    alias VARCHAR(255) NOT NULL,
    language VARCHAR(10) DEFAULT 'zh',
    FOREIGN KEY (entity_id) REFERENCES knowledge_entities(id),
    INDEX idx_alias (alias)
);
```

---

## 🔍 实体提取策略

### 1. 基于目录结构的提取

**从细化后的目录中提取实体：**

```typescript
interface TocItem {
  title: string;
  level: number;
  pageNumber?: number;
  children?: TocItem[];
}

// 提取策略
function extractEntitiesFromToc(toc: TocItem[]): Entity[] {
  const entities: Entity[] = [];
  
  for (const item of toc) {
    // 1. 章节标题 -> 概念实体
    if (item.level <= 2) {
      entities.push({
        id: generateId(),
        name: item.title,
        type: EntityType.CHAPTER,
        source: 'toc',
        level: item.level
      });
    }
    
    // 2. 识别地质术语
    const geologicalTerms = extractGeologicalTerms(item.title);
    geologicalTerms.forEach(term => {
      entities.push({
        id: generateId(),
        name: term,
        type: detectEntityType(term), // 根据术语特征判断类型
        source: 'toc',
        parentChapter: item.title
      });
    });
  }
  
  return entities;
}

// 地质术语识别（使用正则或NLP）
function extractGeologicalTerms(text: string): string[] {
  const patterns = [
    /([\u4e00-\u9fa5]+区系)/g,      // 区系
    /([\u4e00-\u9fa5]+板块)/g,       // 板块
    /([\u4e00-\u9fa5]+带)/g,         // 带
    /([\u4e00-\u9fa5]+运动)/g,       // 运动
    /([\u4e00-\u9fa5]+构造)/g,       // 构造
    /([\u4e00-\u9fa5]+纪)/g,          // 地质年代
    /([\u4e00-\u9fa5]+代)/g           // 地质年代
  ];
  
  const terms: string[] = [];
  patterns.forEach(pattern => {
    const matches = text.match(pattern);
    if (matches) terms.push(...matches);
  });
  
  return [...new Set(terms)]; // 去重
}
```

### 2. 基于内容文本的提取

**从Markdown内容中提取实体和关系：**

```typescript
// 使用NLP技术（如spaCy、jieba + 自定义词典）
async function extractEntitiesFromContent(
  markdownContent: string,
  chapter: string
): Promise<{entities: Entity[], relations: Relation[]}> {
  
  // 1. 文本预处理
  const text = markdownToPlainText(markdownContent);
  
  // 2. 命名实体识别（NER）
  const entities = await performNER(text, {
    domain: 'geology',
    customDictionary: loadGeologyDictionary()
  });
  
  // 3. 关系抽取
  const relations = await extractRelations(text, entities);
  
  // 4. 关联到章节
  entities.forEach(entity => {
    entity.sourceChapter = chapter;
  });
  
  return { entities, relations };
}

// 关系抽取示例
function extractRelations(text: string, entities: Entity[]): Relation[] {
  const relations: Relation[] = [];
  
  // 模式匹配：A导致B、A形成B、A位于B等
  const patterns = [
    {
      regex: /([^。]+)导致([^。]+)/g,
      relationType: RelationType.CAUSES
    },
    {
      regex: /([^。]+)形成([^。]+)/g,
      relationType: RelationType.FORMS
    },
    {
      regex: /([^。]+)位于([^。]+)/g,
      relationType: RelationType.LOCATED_IN
    },
    {
      regex: /([^。]+)属于([^。]+)/g,
      relationType: RelationType.BELONGS_TO
    }
  ];
  
  patterns.forEach(({regex, relationType}) => {
    const matches = text.matchAll(regex);
    for (const match of matches) {
      const source = findEntityByName(match[1], entities);
      const target = findEntityByName(match[2], entities);
      
      if (source && target) {
        relations.push({
          id: generateId(),
          sourceEntityId: source.id,
          targetEntityId: target.id,
          type: relationType,
          sourceText: match[0],
          confidence: 0.8
        });
      }
    }
  });
  
  return relations;
}
```

### 3. AI辅助提取（推荐）

**使用大语言模型进行智能提取：**

```typescript
// 调用LLM API进行实体和关系提取
async function extractWithAI(
  content: string,
  toc: TocItem[]
): Promise<{entities: Entity[], relations: Relation[]}> {
  
  const prompt = `
请从以下地质学文档内容中提取知识图谱：

目录结构：
${JSON.stringify(toc, null, 2)}

文档内容：
${content.substring(0, 5000)}  // 限制长度

请提取：
1. 所有地质实体（构造、现象、位置、年代等）
2. 实体之间的关系
3. 实体的属性

返回JSON格式：
{
  "entities": [
    {
      "name": "实体名称",
      "type": "实体类型",
      "description": "描述",
      "properties": {}
    }
  ],
  "relations": [
    {
      "source": "源实体",
      "target": "目标实体",
      "type": "关系类型",
      "description": "关系描述"
    }
  ]
}
`;

  const response = await callLLMAPI(prompt);
  return parseAIResponse(response);
}
```

---

## 🔗 关系识别方法

### 1. 基于语法模式

```typescript
// 常见的地质学关系模式
const relationPatterns = {
  [RelationType.BELONGS_TO]: [
    /(.+)属于(.+)/,
    /(.+)是(.+)的一部分/,
    /(.+)隶属于(.+)/
  ],
  [RelationType.CAUSES]: [
    /(.+)导致(.+)/,
    /(.+)引起(.+)/,
    /(.+)造成(.+)/
  ],
  [RelationType.LOCATED_IN]: [
    /(.+)位于(.+)/,
    /(.+)处在(.+)/,
    /(.+)分布于(.+)/
  ],
  [RelationType.OCCURS_IN]: [
    /(.+)发生于(.+)/,
    /(.+)形成于(.+)/,
    /(.+)出现在(.+)时期/
  ]
};
```

### 2. 基于共现分析

```typescript
// 统计实体在文档中的共现频率
function findCooccurrenceRelations(
  entities: Entity[],
  content: string
): Relation[] {
  const relations: Relation[] = [];
  const sentences = splitIntoSentences(content);
  
  // 统计在同一句子中出现的实体对
  const cooccurrence = new Map<string, number>();
  
  sentences.forEach(sentence => {
    const entitiesInSentence = entities.filter(e => 
      sentence.includes(e.name)
    );
    
    // 生成实体对
    for (let i = 0; i < entitiesInSentence.length; i++) {
      for (let j = i + 1; j < entitiesInSentence.length; j++) {
        const pair = [entitiesInSentence[i].id, entitiesInSentence[j].id]
          .sort().join('-');
        cooccurrence.set(pair, (cooccurrence.get(pair) || 0) + 1);
      }
    }
  });
  
  // 将高频共现转换为关系
  cooccurrence.forEach((count, pair) => {
    if (count >= 3) { // 阈值
      const [sourceId, targetId] = pair.split('-');
      relations.push({
        id: generateId(),
        sourceEntityId: sourceId,
        targetEntityId: targetId,
        type: RelationType.RELATED_TO,
        confidence: Math.min(count / 10, 1.0) // 归一化置信度
      });
    }
  });
  
  return relations;
}
```

### 3. 基于目录层级

```typescript
// 从目录层级推断关系
function extractRelationsFromToc(toc: TocItem[]): Relation[] {
  const relations: Relation[] = [];
  
  function traverse(items: TocItem[], parentId?: string) {
    items.forEach(item => {
      const itemId = generateIdFromTitle(item.title);
      
      if (parentId) {
        // 子章节属于父章节
        relations.push({
          id: generateId(),
          sourceEntityId: itemId,
          targetEntityId: parentId,
          type: RelationType.BELONGS_TO,
          source: 'toc_hierarchy'
        });
      }
      
      if (item.children) {
        traverse(item.children, itemId);
      }
    });
  }
  
  traverse(toc);
  return relations;
}
```

---

## 🏗️ 技术架构

### 整体架构图

```
┌─────────────────────────────────────────────────────────┐
│                     前端层                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │ 知识图谱可视化 │  │ 实体关系查询  │  │ 图谱编辑管理  │ │
│  │  (D3.js/vis) │  │   (搜索界面)  │  │   (CRUD)     │ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
└─────────────────────────────────────────────────────────┘
                          ↕ HTTP/WebSocket
┌─────────────────────────────────────────────────────────┐
│                      API层                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │ 图谱查询API  │  │ 实体提取API   │  │ 关系推理API  │ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
└─────────────────────────────────────────────────────────┘
                          ↕
┌─────────────────────────────────────────────────────────┐
│                    业务逻辑层                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │ 实体提取服务  │  │ 关系抽取服务  │  │ 图谱构建服务  │ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │ NLP处理服务   │  │ AI辅助提取    │  │ 图谱推理引擎  │ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
└─────────────────────────────────────────────────────────┘
                          ↕
┌─────────────────────────────────────────────────────────┐
│                    数据存储层                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │  图数据库     │  │ 关系数据库    │  │ 文档存储     │ │
│  │  (Neo4j)     │  │  (MySQL)     │  │  (OSS/本地)  │ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
└─────────────────────────────────────────────────────────┘
```

### 与现有系统集成

```typescript
// 在文档解析完成后自动构建知识图谱
async function onDocumentParsed(documentId: string) {
  // 1. 获取解析结果
  const document = await getDocument(documentId);
  const toc = await getToc(documentId);
  const markdownContent = await getMarkdownContent(documentId);
  
  // 2. 提取实体和关系
  const { entities, relations } = await extractKnowledgeGraph({
    toc,
    content: markdownContent,
    documentId
  });
  
  // 3. 保存到知识图谱数据库
  await saveKnowledgeGraph(documentId, entities, relations);
  
  // 4. 更新文档状态
  await updateDocument(documentId, {
    knowledgeGraphStatus: 'completed',
    entityCount: entities.length,
    relationCount: relations.length
  });
}
```

---

## 💾 存储方案

### 方案对比

| 方案 | 优点 | 缺点 | 适用场景 |
|------|------|------|----------|
| **Neo4j** | 原生图查询、性能好、可视化工具 | 需要额外部署、学习成本 | 大规模图谱、复杂查询 |
| **ArangoDB** | 多模型数据库、支持图+文档 | 社区相对小 | 需要灵活数据模型 |
| **MySQL+关系表** | 无需额外部署、易维护 | 图查询性能差、复杂关系难处理 | 小规模图谱、简单关系 |
| **PostgreSQL+JSONB** | 支持JSON查询、性能较好 | 图查询需自己实现 | 中等规模、已有PG环境 |

### 推荐方案：混合存储

```typescript
// 1. 图数据库存储核心图谱（Neo4j）
// 用于复杂查询和可视化
const graphDB = new Neo4jDriver({
  uri: 'bolt://localhost:7687',
  user: 'neo4j',
  password: 'password'
});

// 2. 关系数据库存储元数据（MySQL）
// 用于快速检索和统计
const relationalDB = mysql.createConnection({
  host: 'localhost',
  user: 'root',
  database: 'knowledge_base'
});

// 3. 同步机制
async function syncToRelationalDB(entity: Entity) {
  await relationalDB.query(`
    INSERT INTO knowledge_entities 
    (id, name, type, description, properties)
    VALUES (?, ?, ?, ?, ?)
  `, [entity.id, entity.name, entity.type, entity.description, JSON.stringify(entity.properties)]);
}
```

---

## 🎨 前端可视化

### 1. 图谱可视化组件

**使用 D3.js 或 vis-network**

```vue
<template>
  <div class="knowledge-graph-viewer">
    <div class="graph-controls">
      <button @click="layout = 'force'">力导向布局</button>
      <button @click="layout = 'hierarchical'">层级布局</button>
      <button @click="filterByType">按类型筛选</button>
      <input v-model="searchQuery" placeholder="搜索实体..." />
    </div>
    
    <div ref="graphContainer" class="graph-container"></div>
    
    <div class="entity-panel" v-if="selectedEntity">
      <h3>{{ selectedEntity.name }}</h3>
      <p>{{ selectedEntity.description }}</p>
      <div class="relations">
        <h4>相关关系</h4>
        <div v-for="rel in selectedEntity.relations" :key="rel.id">
          {{ rel.type }} → {{ rel.target.name }}
        </div>
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue';
import * as d3 from 'd3';
import { knowledgeGraphApi } from '@/api/knowledgeGraphApi';

const graphContainer = ref<HTMLElement>();
const selectedEntity = ref<Entity | null>(null);
const searchQuery = ref('');

// 加载图谱数据
const loadGraph = async (documentId: string) => {
  const graph = await knowledgeGraphApi.getGraph(documentId);
  renderGraph(graph);
};

// 渲染图谱
const renderGraph = (graph: KnowledgeGraph) => {
  const width = 1200;
  const height = 800;
  
  const svg = d3.select(graphContainer.value)
    .append('svg')
    .attr('width', width)
    .attr('height', height);
  
  // 创建力导向图
  const simulation = d3.forceSimulation(graph.nodes)
    .force('link', d3.forceLink(graph.links).id(d => d.id))
    .force('charge', d3.forceManyBody().strength(-300))
    .force('center', d3.forceCenter(width / 2, height / 2));
  
  // 绘制边
  const links = svg.append('g')
    .selectAll('line')
    .data(graph.links)
    .enter()
    .append('line')
    .attr('stroke', '#999')
    .attr('stroke-width', 2);
  
  // 绘制节点
  const nodes = svg.append('g')
    .selectAll('circle')
    .data(graph.nodes)
    .enter()
    .append('circle')
    .attr('r', d => Math.sqrt(d.importance) * 5)
    .attr('fill', d => getColorByType(d.type))
    .call(d3.drag()
      .on('start', dragStarted)
      .on('drag', dragged)
      .on('end', dragEnded))
    .on('click', (event, d) => {
      selectedEntity.value = d;
    });
  
  // 添加标签
  const labels = svg.append('g')
    .selectAll('text')
    .data(graph.nodes)
    .enter()
    .append('text')
    .text(d => d.name)
    .attr('font-size', 12)
    .attr('dx', 10)
    .attr('dy', 5);
  
  // 更新位置
  simulation.on('tick', () => {
    links
      .attr('x1', d => d.source.x)
      .attr('y1', d => d.source.y)
      .attr('x2', d => d.target.x)
      .attr('y2', d => d.target.y);
    
    nodes
      .attr('cx', d => d.x)
      .attr('cy', d => d.y);
    
    labels
      .attr('x', d => d.x)
      .attr('y', d => d.y);
  });
};
</script>
```

### 2. 实体详情面板

```vue
<template>
  <div class="entity-detail-panel">
    <div class="entity-header">
      <h2>{{ entity.name }}</h2>
      <span class="entity-type">{{ entity.type }}</span>
    </div>
    
    <div class="entity-description">
      {{ entity.description }}
    </div>
    
    <div class="entity-properties">
      <h3>属性</h3>
      <div v-for="(value, key) in entity.properties" :key="key">
        <strong>{{ key }}:</strong> {{ value }}
      </div>
    </div>
    
    <div class="entity-relations">
      <h3>关系</h3>
      <div v-for="relation in entity.relations" :key="relation.id" 
           class="relation-item"
           @click="navigateToEntity(relation.target)">
        <span class="relation-type">{{ relation.type }}</span>
        <span class="relation-target">{{ relation.target.name }}</span>
      </div>
    </div>
    
    <div class="entity-sources">
      <h3>来源</h3>
      <div v-for="source in entity.sources" :key="source.documentId">
        <a @click="openDocument(source.documentId)">
          {{ source.documentName }} - {{ source.chapter }}
        </a>
      </div>
    </div>
  </div>
</template>
```

---

## 🔌 API设计

### 1. 知识图谱API接口

```typescript
// src/api/knowledgeGraphApi.ts

export interface KnowledgeGraphApi {
  // 获取文档的知识图谱
  getGraph(documentId: string): Promise<KnowledgeGraph>;
  
  // 搜索实体
  searchEntities(query: string, filters?: EntityFilters): Promise<Entity[]>;
  
  // 获取实体详情
  getEntity(entityId: string): Promise<EntityDetail>;
  
  // 获取实体的关系
  getEntityRelations(entityId: string, relationTypes?: string[]): Promise<Relation[]>;
  
  // 获取两个实体之间的路径
  findPath(sourceId: string, targetId: string): Promise<Path>;
  
  // 提取并构建知识图谱
  extractAndBuild(documentId: string): Promise<ExtractionResult>;
  
  // 更新实体
  updateEntity(entityId: string, data: Partial<Entity>): Promise<Entity>;
  
  // 创建关系
  createRelation(relation: CreateRelationRequest): Promise<Relation>;
  
  // 删除实体
  deleteEntity(entityId: string): Promise<void>;
  
  // 获取图谱统计
  getStatistics(documentId?: string): Promise<GraphStatistics>;
}

// 使用示例
const api = knowledgeGraphApi;

// 1. 文档解析后自动构建图谱
await api.extractAndBuild(documentId);

// 2. 可视化图谱
const graph = await api.getGraph(documentId);

// 3. 搜索相关实体
const entities = await api.searchEntities('劳亚区系');

// 4. 查看实体关系
const relations = await api.getEntityRelations('entity_001');

// 5. 查找两个概念之间的关联路径
const path = await api.findPath('劳亚区系', '特提斯陆间带');
```

### 2. 后端API端点

```typescript
// 后端API设计

// POST /api/knowledge-graph/extract
// 从文档中提取知识图谱
{
  "documentId": "doc_001",
  "options": {
    "extractFromToc": true,
    "extractFromContent": true,
    "useAI": true
  }
}

// GET /api/knowledge-graph/:documentId
// 获取文档的知识图谱
Response: {
  "entities": [...],
  "relations": [...],
  "statistics": {...}
}

// GET /api/knowledge-graph/entities/search
// 搜索实体
Query: ?q=劳亚区系&type=geological_structure&limit=10

// GET /api/knowledge-graph/entities/:id
// 获取实体详情

// GET /api/knowledge-graph/entities/:id/relations
// 获取实体的关系

// GET /api/knowledge-graph/path
// 查找路径
Query: ?source=entity_001&target=entity_002&maxDepth=5
```

---

## 🗺️ 实施路线图

### 阶段一：基础框架（2-3周）

1. **数据库设计**
   - [ ] 设计实体和关系表结构
   - [ ] 创建数据库迁移脚本
   - [ ] 设计索引和优化策略

2. **基础API开发**
   - [ ] 实体CRUD接口
   - [ ] 关系CRUD接口
   - [ ] 基础查询接口

3. **简单提取实现**
   - [ ] 从目录提取实体
   - [ ] 基于模式的关系提取
   - [ ] 数据去重和合并

### 阶段二：智能提取（3-4周）

1. **NLP集成**
   - [ ] 集成中文NLP库（jieba、spaCy中文）
   - [ ] 构建地质学词典
   - [ ] 实现命名实体识别

2. **AI辅助提取**
   - [ ] 集成LLM API（OpenAI、Claude等）
   - [ ] 设计提取提示词
   - [ ] 实现批量提取

3. **关系抽取优化**
   - [ ] 实现共现分析
   - [ ] 关系置信度计算
   - [ ] 关系验证机制

### 阶段三：可视化（2-3周）

1. **图谱可视化**
   - [ ] 集成D3.js或vis-network
   - [ ] 实现力导向布局
   - [ ] 实现交互功能（缩放、拖拽、点击）

2. **实体详情面板**
   - [ ] 设计详情展示界面
   - [ ] 实现关系导航
   - [ ] 实现来源追踪

3. **搜索和筛选**
   - [ ] 实现实体搜索
   - [ ] 实现类型筛选
   - [ ] 实现路径查找

### 阶段四：高级功能（3-4周）

1. **图谱推理**
   - [ ] 实现路径查找算法
   - [ ] 实现相似实体推荐
   - [ ] 实现关系推理

2. **图谱编辑**
   - [ ] 实现实体编辑界面
   - [ ] 实现关系编辑
   - [ ] 实现批量操作

3. **统计分析**
   - [ ] 实现图谱统计面板
   - [ ] 实现实体重要性分析
   - [ ] 实现关系强度分析

### 阶段五：优化和扩展（持续）

1. **性能优化**
   - [ ] 图谱查询优化
   - [ ] 缓存机制
   - [ ] 增量更新

2. **功能扩展**
   - [ ] 多文档图谱合并
   - [ ] 图谱版本管理
   - [ ] 导出功能（JSON、GraphML）

---

## 📝 示例场景

### 场景1：从"板块构造与地貌形迹"构建图谱

**输入：**
- 目录结构（细化后的TOC）
- Markdown内容

**处理流程：**

1. **目录提取**
   ```
   第一章：劳亚区系
   ├─ 1.1 劳亚区系概述
   ├─ 1.2 劳亚区系的地质特征
   └─ 1.3 劳亚区系的演化
   ```

2. **提取的实体**
   - 劳亚区系（GeologicalStructure）
   - 冈瓦纳区系（GeologicalStructure）
   - 特提斯陆间带（Location）
   - 板块构造理论（Theory）

3. **提取的关系**
   - 劳亚区系 `属于` 泛大陆
   - 劳亚区系 `位于` 北半球
   - 特提斯陆间带 `分离` 劳亚区系和冈瓦纳区系
   - 板块运动 `导致` 造山运动

4. **可视化结果**
   - 节点：实体（不同颜色表示不同类型）
   - 边：关系（不同样式表示不同类型）
   - 点击节点显示详情和关系

### 场景2：知识查询

**用户操作：**
1. 搜索"劳亚区系"
2. 查看其所有关系
3. 点击"特提斯陆间带"查看详情
4. 查找从"劳亚区系"到"太平洋板块"的路径

**系统响应：**
- 显示实体详情
- 展示所有相关关系
- 高亮显示查询路径
- 提供相关文档链接

---

## 🎯 总结

### 核心优势

1. **自动化提取**：从文档解析结果自动构建知识图谱
2. **智能识别**：结合NLP和AI提高提取准确性
3. **可视化展示**：直观的图谱可视化界面
4. **知识关联**：发现文档中隐含的知识关联
5. **可扩展性**：支持多文档、多领域的图谱构建

### 技术选型建议

- **存储**：Neo4j（图数据库）+ MySQL（元数据）
- **NLP**：jieba（中文分词）+ 自定义地质词典
- **AI**：OpenAI GPT-4 / Claude（实体和关系提取）
- **可视化**：D3.js 或 vis-network
- **前端**：Vue 3 + TypeScript（与现有系统一致）

### 下一步行动

1. 确定存储方案（图数据库 vs 关系数据库）
2. 设计详细的数据库Schema
3. 实现基础的实体提取功能
4. 开发简单的可视化界面
5. 逐步集成AI和NLP功能

---

## 📚 参考资料

- [Neo4j 官方文档](https://neo4j.com/docs/)
- [D3.js 图可视化教程](https://observablehq.com/@d3/gallery)
- [知识图谱构建最佳实践](https://www.kgbase.cn/)
- [中文NLP工具对比](https://github.com/fighting41love/funNLP)

---

**如有任何问题，欢迎讨论！** 🚀


